# Notion File Backup 备份架构设计

## 1. 设计目标

这个程序不是目录镜像或双向同步工具，而是面向恢复的增量备份工具。优先级依次为：

1. 旧版本不可被一次误删除覆盖。
2. 中断后可继续，不重复上传已经完成的对象。
3. 每次快照都有完整、可验证的恢复清单。
4. 相同文件内容只保存一次，降低 API 请求和存储消耗。
5. Token、绝对本地路径等敏感信息不写入前端或远端清单。

## 2. 数据模型

### Blob 对象

每个文件计算 SHA-256。哈希作为对象唯一标识，远端页面保存原始文件、SHA-256、相对路径、文件大小和 MIME 类型。本地 SQLite 表保存 `sha256 → page_id / block_id`，上传并挂载成功后立即落库，后续步骤失败也可以复用对象。

### Snapshot 快照

每次有效备份生成完整 `manifest.json`，包含快照 ID、创建时间、相对路径、SHA-256、大小、修改时间、Blob 页面和文件块 ID，以及被规则跳过的文件与原因。

快照页面只追加，不覆盖旧快照。本地删除意味着该路径不再出现在新清单中，旧清单仍然引用原对象。

## 3. 一致性边界

1. 扫描目录并对变化文件计算 SHA-256。
2. 检查本地 Blob 索引。
3. 上传缺失对象，并立即保存 Blob 索引。
4. 生成完整快照清单。
5. 上传清单并创建远端快照页面。
6. 快照成功后，更新本地“当前文件”索引和历史记录。

如果在对象上传阶段中断，已经挂载的对象仍可在下一次运行中复用。如果在快照创建前中断，不会出现一个被标记为成功但缺少清单的快照。

## 4. 恢复流程

1. 从本地历史定位快照页面和 manifest 文件块。
2. 通过 Notion API 获取 manifest 的临时下载地址。
3. 逐项获取 Blob 文件块的最新临时下载地址。
4. 下载到同目录临时文件。
5. 校验长度与 SHA-256。
6. 校验成功后重命名到目标路径，并恢复修改时间。

相对路径会进行路径穿越检查，禁止绝对路径、`..` 和平台前缀。

## 5. 删除与远端垃圾回收

默认永不自动删除远端 Blob。未来的垃圾回收必须枚举全部保留快照、建立引用集合，只把零引用对象列为候选，并提供预览、保留期和二次确认。

## 6. 失败与重试

- 客户端串行节流到约 360ms/请求。
- HTTP 429 与 5xx 按 `Retry-After` 或指数退避重试。
- 小文件使用单段上传，大文件使用 10 MiB 分片并执行 complete。
- 扫描存在不可读文件时，不创建可能被误认为完整的快照。
- 用户取消后保留已完成 Blob，不更新当前快照索引。

## 7. 安全边界

- Token 使用系统凭据库，不回传给 React 前端。
- 清单只包含相对路径，不包含源目录绝对路径。
- 日志和错误信息不输出 Token。
- 当前不提供端到端或零知识加密；敏感资料应在上传前使用独立加密方案。

## 8. 后续阶段

- 从任意远端快照页面 ID 重建本地索引。
- Windows 任务计划与托盘定时备份。
- 可选客户端加密和密钥恢复包。
- 远端零引用对象审计与安全清理。
- 多备份任务、多 Notion 工作区配置。
- 定期抽样恢复验证。
